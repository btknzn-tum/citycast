# CLAUDE.local.md — Personal Instructions

## Project
- Solo project, no team. Keep workflows simple — no need for PR reviews or branch conventions.
- Stack not chosen yet. Don't assume a framework or language until one is added.

## Coding Preferences
- Every new function must have a local test written alongside it.
- Keep solutions simple and minimal — avoid over-engineering.

## Automation (already configured)
- Bash commands are logged to `~/.claude/bash-log.txt`
- macOS notification fires when Claude stops
- Agent auto-writes tests after every file edit
- Weekly Sunday commit + push to remote (session cron)

## Memory
- Learn from corrections and preferences and save them to memory.
- Check memory at the start of each session for this project.
